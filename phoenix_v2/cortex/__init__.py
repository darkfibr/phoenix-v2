"""Phoenix v2 Cortex backend.

Pluggable storage layer ported from Jackson's Cortex repo:
    - vector_store.py   — binary BLOB storage, cosine similarity
    - graph_store.py    — entities, relations, bidirectional queries
    - episodic.py       — session reconstruction from memories
"""